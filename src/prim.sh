# Similar to EZserver lets you download/run jars and stuff

#locals lol
export SCRIPT_DIR=$( cd "$( dirname "$0" )" && pwd ) #lol batch does this automaticly, windows better this will be very annoying in the future
